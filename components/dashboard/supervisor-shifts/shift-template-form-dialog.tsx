"use client";
import React, { useState, useTransition } from "react";
import { createShiftTemplate, updateShiftTemplate } from "@/features/supervisor-shifts/actions";
import type { ToastState } from "@/components/dashboard/users/user-toast";
import type { Dictionary } from "@/i18n/dictionaries";
type ShiftTemplateFormData = { name: string; start_time: string; end_time: string; work_days: number[]; is_active: boolean; has_break: boolean; break_start_time: string; break_end_time: string };
type Props = { open: boolean; onOpenChange: (open: boolean) => void; shiftToEdit?: any | null; onToast: (toast: ToastState) => void; dictionary?: Dictionary["dashboard"]["supervisorShifts"] };
export function ShiftTemplateFormDialog({ open, onOpenChange, shiftToEdit, onToast, dictionary }: Props) {
  const [isPending, startTransition] = useTransition(); const text = dictionary!.dialogs; const isEdit = !!shiftToEdit; const [form, setForm] = useState<ShiftTemplateFormData>({ name: shiftToEdit?.name || "", start_time: shiftToEdit?.start_time?.substring(0, 5) || "09:00", end_time: shiftToEdit?.end_time?.substring(0, 5) || "17:00", work_days: shiftToEdit?.work_days || [], is_active: shiftToEdit ? shiftToEdit.is_active : true, has_break: shiftToEdit?.break_start_time != null, break_start_time: shiftToEdit?.break_start_time?.substring(0, 5) || "12:00", break_end_time: shiftToEdit?.break_end_time?.substring(0, 5) || "13:00" });
  React.useEffect(() => { if (open) setForm({ name: shiftToEdit?.name || "", start_time: shiftToEdit?.start_time?.substring(0, 5) || "09:00", end_time: shiftToEdit?.end_time?.substring(0, 5) || "17:00", work_days: shiftToEdit?.work_days || [], is_active: shiftToEdit ? shiftToEdit.is_active : true, has_break: shiftToEdit?.break_start_time != null, break_start_time: shiftToEdit?.break_start_time?.substring(0, 5) || "12:00", break_end_time: shiftToEdit?.break_end_time?.substring(0, 5) || "13:00" }); }, [open, shiftToEdit]);
  if (!open) return null;
  const submit = (event: React.FormEvent) => { event.preventDefault(); if (!form.name.trim() || form.work_days.length === 0) { onToast({ message: text.unexpectedError, tone: "error" }); return; } startTransition(async () => { try { const data = { ...form, break_start_time: form.has_break ? form.break_start_time : undefined, break_end_time: form.has_break ? form.break_end_time : undefined }; if (isEdit) await updateShiftTemplate(shiftToEdit.id, data); else await createShiftTemplate(data); onToast({ message: text.success, tone: "success" }); onOpenChange(false); } catch (error: any) { onToast({ message: error.message || text.unexpectedError, tone: "error" }); } }); };
  const days = dictionary!.days.map((label, value) => ({ label, value }));
  return <div className="fixed inset-0 z-[60] flex items-center justify-center bg-black/50 p-4"><div className="w-full max-w-md rounded-xl bg-white p-6 shadow-lg"><div className="mb-6 flex items-center justify-between"><h2 className="text-xl font-bold text-navy">{text.shiftName}</h2><button onClick={() => onOpenChange(false)} className="text-muted">✕</button></div><form onSubmit={submit} className="space-y-4"><label className="block text-sm font-medium text-navy">{text.shiftName}<input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} className="mt-1 w-full rounded-md border border-border px-3 py-2 text-sm" /></label><div className="grid grid-cols-2 gap-4"><label className="text-sm font-medium text-navy">{text.startTime}<input type="time" required value={form.start_time} onChange={(e) => setForm({ ...form, start_time: e.target.value })} className="mt-1 w-full rounded-md border border-border px-3 py-2 text-sm" /></label><label className="text-sm font-medium text-navy">{text.endTime}<input type="time" required value={form.end_time} onChange={(e) => setForm({ ...form, end_time: e.target.value })} className="mt-1 w-full rounded-md border border-border px-3 py-2 text-sm" /></label></div><label className="flex items-center gap-2 text-sm font-medium text-navy"><input type="checkbox" checked={form.has_break} onChange={(e) => setForm({ ...form, has_break: e.target.checked })} />{text.hasBreak}</label>{form.has_break ? <div className="grid grid-cols-2 gap-4"><label className="text-sm font-medium text-navy">{text.breakStart}<input type="time" required value={form.break_start_time} onChange={(e) => setForm({ ...form, break_start_time: e.target.value })} className="mt-1 w-full rounded-md border border-border px-3 py-2 text-sm" /></label><label className="text-sm font-medium text-navy">{text.breakEnd}<input type="time" required value={form.break_end_time} onChange={(e) => setForm({ ...form, break_end_time: e.target.value })} className="mt-1 w-full rounded-md border border-border px-3 py-2 text-sm" /></label></div> : null}<div><p className="mb-2 text-sm font-medium text-navy">{text.workingDays}</p><div className="flex flex-wrap gap-2">{days.map((day) => <button key={day.value} type="button" onClick={() => setForm({ ...form, work_days: form.work_days.includes(day.value) ? form.work_days.filter((value) => value !== day.value) : [...form.work_days, day.value] })} className={`rounded-full px-3 py-1 text-sm ${form.work_days.includes(day.value) ? "bg-primary text-white" : "bg-surface text-navy"}`}>{day.label}</button>)}</div></div><label className="flex items-center gap-2 text-sm font-medium text-navy"><input type="checkbox" checked={form.is_active} onChange={(e) => setForm({ ...form, is_active: e.target.checked })} />{text.enabled}</label><div className="flex justify-end gap-3 border-t pt-4"><button type="button" onClick={() => onOpenChange(false)} className="rounded-md bg-surface px-4 py-2 text-sm">{text.cancel}</button><button type="submit" disabled={isPending} className="rounded-md bg-primary px-4 py-2 text-sm text-white">{isPending ? text.saving : text.save}</button></div></form></div></div>;
}
