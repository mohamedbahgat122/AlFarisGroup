"use client";
import React, { useState, useTransition } from "react";
import { setSupervisorWeeklyOff } from "@/features/supervisor-shifts/actions";
import type { ToastState } from "@/components/dashboard/users/user-toast";
import type { Dictionary } from "@/i18n/dictionaries";
type Props = { open: boolean; onOpenChange: (open: boolean) => void; supervisor: any | null; allSupervisors: any[]; currentWeeklyOff: any | null; onToast: (toast: ToastState) => void; dictionary?: Dictionary["dashboard"]["supervisorShifts"] };
export function EditWeeklyOffDialog({ open, onOpenChange, supervisor, allSupervisors, currentWeeklyOff, onToast, dictionary }: Props) { const [isPending, startTransition] = useTransition(); const [dayOfWeek, setDayOfWeek] = useState(5); const [coverageId, setCoverageId] = useState(""); const [startDate, setStartDate] = useState(""); const [notes, setNotes] = useState(""); const text = dictionary!.dialogs; React.useEffect(() => { if (open && supervisor) { setDayOfWeek(currentWeeklyOff?.day_of_week ?? 5); setCoverageId(currentWeeklyOff?.coverage_supervisor_id || ""); setStartDate(new Date().toISOString().split("T")[0]); setNotes(""); } }, [open, supervisor, currentWeeklyOff]); if (!open || !supervisor) return null; const submit = (event: React.FormEvent) => { event.preventDefault(); if (dayOfWeek < 0 || dayOfWeek > 6 || !startDate) { onToast({ message: text.effectiveRequired, tone: "error" }); return; } startTransition(async () => { try { await setSupervisorWeeklyOff({ supervisor_id: supervisor.id, day_of_week: dayOfWeek, coverage_supervisor_id: coverageId || null, effective_from: startDate, notes: notes || undefined }); onToast({ message: text.success, tone: "success" }); onOpenChange(false); } catch (error: any) { onToast({ message: error.message || text.unexpectedError, tone: "error" }); } }); }; return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4"><div className="w-full max-w-md rounded-xl bg-white p-6 shadow-lg"><div className="mb-6 flex items-center justify-between"><h2 className="text-xl font-bold text-navy">{text.weeklyOffTitle}</h2><button onClick={() => onOpenChange(false)} className="text-muted">✕</button></div><form onSubmit={submit} className="space-y-4"><label className="block text-sm font-medium text-navy">{dictionary!.supervisor}<input value={supervisor.full_name} readOnly className="mt-1 w-full rounded-md border border-border bg-surface px-3 py-2 text-sm" /></label><label className="block text-sm font-medium text-navy">{dictionary!.weeklyOff}<select value={dayOfWeek} onChange={(e) => setDayOfWeek(Number(e.target.value))} className="mt-1 w-full rounded-md border border-border px-3 py-2 text-sm">{dictionary!.days.map((day, value) => <option key={value} value={value}>{day}</option>)}</select></label><label className="block text-sm font-medium text-navy">{text.coverage}<select value={coverageId} onChange={(e) => setCoverageId(e.target.value)} className="mt-1 w-full rounded-md border border-border px-3 py-2 text-sm"><option value="">{text.noCoverage}</option>{allSupervisors.filter((item) => item.id !== supervisor.id).map((item) => <option key={item.id} value={item.id}>{item.full_name}</option>)}</select></label><label className="block text-sm font-medium text-navy">{text.effectiveDate}<input type="date" required value={startDate} onChange={(e) => setStartDate(e.target.value)} className="mt-1 w-full rounded-md border border-border px-3 py-2 text-sm" /></label><label className="block text-sm font-medium text-navy">{text.notes}<textarea value={notes} onChange={(e) => setNotes(e.target.value)} className="mt-1 min-h-20 w-full rounded-md border border-border px-3 py-2 text-sm" /></label><div className="flex justify-end gap-3 border-t pt-4"><button type="button" onClick={() => onOpenChange(false)} className="rounded-md bg-surface px-4 py-2 text-sm">{text.cancel}</button><button type="submit" disabled={isPending} className="rounded-md bg-primary px-4 py-2 text-sm text-white">{isPending ? text.saving : text.save}</button></div></form></div></div>; }
